// Interactive Scene
// Demo


function setup() {
  createCanvas(windowWidth, windowHeight);
}

function draw() {
  background(0);
  circle(mouseX, mouseY, 100)
  console.log(1)
}

let arrX = (0, 1, 2)
let arrY = [arrX, arrX, arrX]